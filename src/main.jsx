import React from 'react'
import ReactDOM from 'react-dom/client'
import EmailJsonGeneratorUI from './EmailJsonGeneratorUI.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EmailJsonGeneratorUI />
  </React.StrictMode>,
)